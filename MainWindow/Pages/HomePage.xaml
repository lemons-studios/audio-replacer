<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AudioReplacer.MainWindow.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:data="using:AudioReplacer.MainWindow.PageData"
    xmlns:md="using:CommunityToolkit.Labs.WinUI.MarkdownTextBlock"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnDefinitions="*, *" VerticalAlignment="Stretch">
        <Grid Column="0" RowDefinitions="*, *" Margin="0 0 0 10" VerticalAlignment="Stretch">
            <Border 
                Grid.Row="0" 
                Style="{StaticResource PanelCard}" Padding="15" Margin="10">
                <StackPanel>
                    <TextBlock 
                        Style="{StaticResource TitleText}"
                        FontSize="40"
                        Text="Projects"/>
                </StackPanel>
            </Border>
            <Border Grid.Row="1" Style="{StaticResource PanelCard}" Padding="15" Margin="10 10 10 0">
                <TextBlock 
                    Style="{StaticResource TitleText}"
                    FontSize="40"
                    Text="Stats"/>
            </Border>
        </Grid>
        <Grid Column="1" RowDefinitions="*">
            <Border Style="{StaticResource PanelCard}" Padding="15" Margin="10">
                <StackPanel>
                    <TextBlock 
                        Style="{StaticResource TitleText}"
                        FontSize="40"
                        Text="Latest Changes:"
                        Margin="0 0 0 10"/>
                    <TextBlock Text="Test"/>
                    <md:MarkdownTextBlock Text="# No Release Logs Available." />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
